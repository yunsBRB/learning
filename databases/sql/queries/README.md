# Requêtes

- `student-preview.sql` : aperçu des étudiants dans `dbslide`.
- `select-joins-and-aggregates.sql` : sélections, filtres, dates, jointures et agrégations.

Les requêtes sont des essais de formation ; elles n’ont pas été corrigées ni exécutées contre une base complète lors du rangement.
